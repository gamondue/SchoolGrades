Vorrei sincronizzare il funzionamento del TreeMptt per Windows Forms con quello per WPF. Le classi per WPF devono diventare uguali a quella per Windows Forms, fatto salvo naturalmente il fatto di usare controlli di tecnologie diverse. Le classi per Windows Forms non devono essere modificate, si modificano solo le classi WFP che gestiscono la visualizzazine, la modifica ed il salvataggio su database dell'albero. Naturalmente nulla deve cambiare nell'accesso API all'albero, che deve diventare identico all'accesso che fa ora il programma Windows Forms. Per rispettare le modifiche alla API di Windows Forms potrai modificare anche le finestre WPF che utilizzano l'albero.

Vorrei sincronizzare il comportamento e la UI del frmSetup per Windows Forms con quello per WPF. La UI per WPF deve diventare uguale a quella per Windows Forms, spostando i bottoni che ci sono già per metterli nello stesso ordine del Win Forms. Il programma per Windows Forms non deve essere modificato, si modificano solo le classi WFP che gestiscono la pagina.